# HWP 반자동화 파이프라인 의존성 (실제 설치 검증 완료)
# 설치: pip3 install --user --break-system-packages -r requirements.txt  (WSL2/Ubuntu)
python-hwpx-automation>=7.0.3   # 내용 주입(paragraph_patch) + 읽기 + 검증 (실제 사용)
syhwp>=0.0.7                    # 보조 파서 (선택)

# ---- Windows 전용 (구조 변경: 표 병합/행 추가) ----
pyhwpx>=1.7; sys_platform == 'win32'
