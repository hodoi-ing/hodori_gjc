# HWP 자동화 파이프라인 의존성
# 설치: pip install -r requirements.txt
# 참고: 표 구조(OLE) 조작은 Windows + 아래아한글 설치 필수

# --- XML 엔진 (OS 무관: 텍스트/누름틀 치환) ---
python-hwpx-automation>=0.1.0
hwpx-kit>=0.1.0
syhwp>=0.1.0

# --- OLE 엔진 (Windows 전용: 표 구조/서식 조작, 한컴오피스 필요) ---
pyhwpx>=0.1.0

# --- 안전성/동시성 ---
filelock>=3.0           # OLE 싱글톤 워커 락 (멀티쓰레딩 충돌 방지)
func-timeout>=4.0       # 팝업 무한 대기 방지용 taskkill 타임아웃 래퍼
